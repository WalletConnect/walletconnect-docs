import Tabs from '@theme/Tabs'
import TabItem from '@theme/TabItem'

To open Web3Modal you can use our [**web component**](../../core/components.mdx) or build your own button with Web3Modal [**composables**](../../core/composables.mdx#useweb3modal).
In this example we are going to use the `<w3m-button>` component.

Web components are global html elements that don't require importing.

```html
<template>
  <w3m-button />
</template>
```

Learn more about the Web3Modal web components [here](../../core/components.mdx)
