# Actions

:::caution
This documentation is for Web3Modal v2. You can find Web3Modal v3 docs [here](../../../about.mdx)
:::

## openModal

Programmatically open the modal. Optionally, override default route to open.

### Example

```js
await web3modal.openModal()
```

### Reference

```ts
openModal: (options?: Options) => Promise<void>
```

```ts
// Options that can be passed to override default route
interface Options {
  route?: 'Account' | 'ConnectWallet' | 'Help' | 'SelectNetwork'
}
```

## closeModal

Programmatically close the modal.

### Example

```js
web3modal.closeModal()
```

### Reference

```ts
closeModal: () => void
```

## subscribeModal

Subscribe or unsubscribe from modal's state.

### Example

```js
const unsubscribe = web3modal.subscribeModal(newState => console.log(newState))
unsubscribe()
```

### Reference

```ts
subscribeModal: (callback: (newState: Object) => void) => () => void
```

## setDefaultChain

Sets the default chain before the user connects.

### Example

```js
web3modal.setDefaultChain(polygon)
```

### Reference

```ts
setDefaultChain: (chain: WagmiChain) => void
```

## setTheme

Programmatically set or update theme options for [`themeVariables`](./options#themevariables-optional) and [`themeMode`](./options#thememode-optional).

### Example

```js
web3modal.setTheme({
  themeMode: 'dark',
  themeVariables: {
    '--w3m-font-family': 'Roboto, sans-serif',
    '--w3m-accent-color': '#F5841F'
    // ...
  }
})
```

### Reference

```ts
setTheme: (theme: Theme) => void
```

```ts
interface Theme {
  themeMode: 'dark' | 'light'
  themeVariables: Object
}
```

## Wagmi Actions

Other actions like [`getAccount`](https://wagmi.sh/core/actions/getAccount), [`getContract`](https://wagmi.sh/core/actions/getContract) and many more are available from [wagmi](https://wagmi.sh/core/getting-started).
