# Actions

## connect

Action to create connection request with given options. Returns session data.

### Example

```js
const data = await web3modal.connect(ConnectOptions)
```

### Reference

```ts
connect: (options: Namespaces) => Promise<Session>
```
