# Components

## Web3ModalAuth

Core component that should be placed at the root of your app. See [options](./options) docs for available props.

```tsx
import { Web3ModalAuth } from '@web3modal/auth-react'

return <Web3ModalAuth projectId="" metadata={} modalOptions={} />
```
