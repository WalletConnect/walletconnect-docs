import WcModalThemeVariablesTable from '../../_partials/wcModalThemeVariablesTable.mdx'
import ThemeModePartial from '../../../web3modal/_partials/themeMode.mdx'
import ThemeVariablesPartial from '../../../web3modal/_partials/themeVariables.mdx'

# Theming

## themeMode

<ThemeModePartial />

### Example

```js
const modal = new WalletConnectModalAuth({
  modalOptions: {
    themeMode: 'dark'
  }
})
```

## themeVariables

<ThemeVariablesPartial />

### Example

```js
const modal = new WalletConnectModalAuth({
  modalOptions: {
    themeVariables: {
      '--w3m-font-family': 'Roboto, sans-serif',
      '--w3m-accent-color': '#F5841F'
    }
  }
})
```

### List of theme variables

<WcModalThemeVariablesTable />
