# Components

## WalletConnectModalAuth

Core component that should be placed at the root of your app. See [options](./options) docs for available props.

```tsx
import { WalletConnectModalAuth } from '@walletconnect/modal-auth-react'

return <WalletConnectModalAuth projectId="" metadata={} modalOptions={} />
```
