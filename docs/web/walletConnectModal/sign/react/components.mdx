# Components

## WalletConnectModalSign

Core component that should be placed at the root of your app. See [options](./options) docs for available props.

```tsx
import { WalletConnectModalSign } from '@walletconnect/modal-sign-react'

return <WalletConnectModalSign projectId="" metadata={} modalOptions={} />
```
